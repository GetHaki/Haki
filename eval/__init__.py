"""Haki public evaluation harness (sprint 10).

Reproducible benchmarks on LongMemEval_S and LoCoMo: frozen configs
(dataset + sha256, models, prompts, budgets, prices), both systems
(Haki vs full-context baseline) re-run in the SAME protocol, honest
metrics (accuracy per type, abstention, contradiction leakage, tokens,
latency, cost). See eval/README.md.
"""
